// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

contract Arithmetic {
    // Function to check if a number is odd
    function isOdd(uint256 number) public pure returns (bool) {
        return number % 2 != 0;
    }

    // Function to check if a number is even
    function isEven(uint256 number) public pure returns (bool) {
        return number % 2 == 0;
    }

    // Function to find the most significant bit of a number
    function mostSignificantBit(uint256 number) public pure returns (uint256) {
        uint256 msb = 0;
        while (number > 0) {
            msb = number;
            number >>= 1;
        }
        return msb;
    }
}
